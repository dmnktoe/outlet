dist: trusty

language: node_js
node_js:
  - '10'

addons:
  chrome: stable

cache:
  directories:
    - ./node_modules

install:
  - npm install

script:
  - npm run test:ci
  - npm run build
  - bash sentry-deploy.sh

after_success:
  - codecov
